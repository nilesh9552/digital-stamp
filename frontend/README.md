# Digital Stamp frontend

React + Vite customer, shop-owner, and super-admin dashboards.

```bash
cd frontend
npm install
npm run dev
```

The Vite dev server proxies `/api` to `http://localhost:8080`.
