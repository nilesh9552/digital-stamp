# Digital Stamp backend

Spring Boot 3 REST API for the Digital Stamp loyalty platform.

## Run

```bash
# from repo root, with MySQL available
cd backend
mvn spring-boot:run
```

Environment variables are documented in the root `.env.example`.

## Tests

```bash
mvn clean test
```
